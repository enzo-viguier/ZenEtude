{% extends 'base.html.twig' %}

{% block title %}Hello AccountController!{% endblock %}

{% block body %}

    {{ include('_nav.html.twig') }}

    <section>
        <div class="px-8 py-24 mx-auto md:px-12 lg:px-32 max-w-7xl">
            <div class="text-center">
                <div>
                    <p class="text-4xl font-semibold tracking-tighter text-gray-900">
                        Conseils
                    </p>
                    <p class="w-1/2 mx-auto mt-4 text-base font-medium text-gray-500 text-balance">
                        Prend de bonnes habitudes
                    </p>
                </div>
            </div>
            <div class="max-w-3xl mx-auto mt-12">
                <div x-data="{
                activeAccordion: '',
                setActiveAccordion(id) {
                this.activeAccordion = (this.activeAccordion == id) ? '' : id
                }
                }">
                    <div class="cursor-pointer group text-gray-600 hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Établir une routine régulière</span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none;">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                Une routine quotidienne peut aider à apporter une structure et à réduire l'anxiété. Inclure des activités telles que des études régulières, des repas équilibrés, et un temps de détente.
                            </div>
                        </div>
                    </div>
                    <div class="text-gray-600 cursor-pointer group hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Gérer le temps efficacement</span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                Utiliser des outils de gestion du temps comme des agendas ou des applications pour planifier les études et les pauses. Cela peut aider à éviter la surcharge et le stress de dernière minute.
                            </div>
                        </div>
                    </div>
                    <div class="text-gray-600 cursor-pointer group hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Pratiquer des activités physiques </span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                L'exercice régulier, même modéré, peut grandement améliorer la santé mentale en réduisant les niveaux de stress et en augmentant les endorphines, les "hormones du bonheur".
                            </div>
                        </div>
                    </div>
                    <div class="text-gray-600 cursor-pointer group hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Adopter une alimentation saine</span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                Manger des aliments nutritifs peut affecter positivement l'humeur et l'énergie. Éviter une consommation excessive de caféine et de sucre.
                            </div>
                        </div>
                    </div>
                    <div class="text-gray-600 cursor-pointer group hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Prioriser le sommeil</span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                Assurer un sommeil suffisant est essentiel pour la récupération du cerveau et la régulation des émotions.
                            </div>
                        </div>
                    </div>
                    <div class="text-gray-600 cursor-pointer group hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Cultiver des relations sociales</span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                Maintenir des relations avec des amis et la famille peut fournir un soutien émotionnel vital.
                            </div>
                        </div>
                    </div>
                    <div class="text-gray-600 cursor-pointer group hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Pratiquer la pleine conscience et la relaxation</span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                Techniques comme la méditation, le yoga, ou des exercices de respiration peuvent aider à gérer le stress et l'anxiété.
                            </div>
                        </div>
                    </div>
                    <div class="text-gray-600 cursor-pointer group hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Demander de l'aide quand c'est nécessaire </span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                Ne pas hésiter à parler à des conseillers d'orientation, des psychologues, ou des services de santé mentale disponibles dans l'établissement.
                            </div>
                        </div>
                    </div>
                    <div class="text-gray-600 cursor-pointer group hover:text-gray-500" x-data="{ id: $id('accordion') }" :class="{ 'text-gray-900': activeAccordion==id, 'text-gray-600 hover:text-gray-500': activeAccordion!=id }">
                        <button class="flex items-center justify-between w-full p-4 pb-1 text-lg font-medium text-black select-none" @click="setActiveAccordion(id)">
                            <span>Limiter l'exposition aux écrans avant de dormir </span><svg class="size-5 text-gray-500 duration-300 ease-out" fill="none" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" stroke="currentColor" stroke-width="1.5" :class="{ '-rotate-[45deg]': activeAccordion==id }">
                                <path d="M12 6v12m6-6H6" stroke-linecap="round" stroke-linejoin="round"></path>
                            </svg>
                        </button>
                        <div x-collapse="" x-show="activeAccordion==id" style="display: none">
                            <div class="p-4 pt-2 text-base font-medium text-gray-500 text-balance">
                                Réduire l'utilisation des appareils électroniques avant le coucher pour améliorer la qualité du sommeil.
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>


    {{ include('_footer.html.twig') }}

{% endblock %}